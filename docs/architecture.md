# Architecture

- `public/` contains static pages and deploy-ready assets.
- `src/` contains source-authoring layers for styles, scripts, and content.
- `scripts/` contains utility scripts for validation and cleanup.
